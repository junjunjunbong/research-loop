# Bootstrap existing projects instead of requiring domain modules

Research Loop compiles a project-specific Research Profile from the user's goal and repository evidence. Domain modules are not required because making users author one before a loop can run contradicts the product goal; reusable research knowledge may be added later only as optional input to hypothesis generation.

